{
  "name": "11ty-cirrus",
  "version": "1.0.0",
  "description": "A minimal 11ty theme",
  "main": "index.js",
  "scripts": {
    "dev": "eleventy --serve",
    "clean": "rm -r _site/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "artze",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
